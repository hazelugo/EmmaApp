---
phase: 08-more-mascots
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/App.vue
autonomous: false
requirements:
  - MASCOT-01
must_haves:
  truths:
    - "App.vue passes :stars=\"stars\" to <CharacterSelect /> so unlock state reacts to the live star count"
    - "With emma-stars=0 in localStorage, only the original 4 characters are selectable on character select screen"
    - "With emma-stars=200 in localStorage, all 10 characters are selectable"
    - "Selecting a new character (yoshi/luigi/bowser/birdo/toadette/boo) does not throw or produce console errors; title theme plays"
    - "No new localStorage keys are written when characters are 'unlocked' (only emma-stars drives unlock state)"
  artifacts:
    - path: "src/App.vue"
      provides: "Wires reactive stars ref into CharacterSelect via :stars prop"
      contains: ":stars=\"stars\""
  key_links:
    - from: "src/App.vue"
      to: "src/components/CharacterSelect.vue"
      via: "stars prop binding on <CharacterSelect>"
      pattern: ":stars=\"stars\""
---

<objective>
Wire the reactive `stars` ref from `useMathGame` into `<CharacterSelect>` so unlock thresholds light up live, then human-verify the full Phase 8 experience.

Purpose: Complete MASCOT-01 by closing the data-flow loop — Plan 01 added the prop and unlock logic; this plan supplies the value and validates end-to-end behavior.
Output: 1-line prop binding in App.vue + signed-off human-verify checkpoint covering grid, lock badges, and unlock at thresholds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-more-mascots/08-RESEARCH.md
@.planning/phases/08-more-mascots/08-01-PLAN.md
@src/App.vue
@src/components/CharacterSelect.vue

<interfaces>
<!-- Current state in App.vue, verified by direct read. -->

App.vue already destructures `stars` from useMathGame (line 27):
```js
const { stars, streak, problemKey, ... } = useMathGame()
```

App.vue currently passes NO props to CharacterSelect (lines 408-412):
```html
<Transition name="fade">
  <CharacterSelect
    v-if="!selectedCharacter"
    @select="onSelectCharacter"
  />
</Transition>
```

Plan 01 makes CharacterSelect accept a `stars: Number` prop (default 0). This plan must add the binding `:stars="stars"`.

useMathGame persists stars to localStorage key `emma-stars`. Setting `localStorage.setItem('emma-stars', '200')` and reloading is the manual test pattern.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass :stars prop to CharacterSelect in App.vue</name>
  <files>src/App.vue</files>
  <read_first>
    - src/App.vue (lines 1-50 for imports and useMathGame destructure; lines 405-415 for CharacterSelect mount site)
    - .planning/phases/08-more-mascots/08-01-PLAN.md (confirms prop name and shape on CharacterSelect)
  </read_first>
  <action>
In `src/App.vue`, locate the `<CharacterSelect>` element (around lines 408-412):

```html
<CharacterSelect
  v-if="!selectedCharacter"
  @select="onSelectCharacter"
/>
```

Add the `:stars="stars"` binding:

```html
<CharacterSelect
  v-if="!selectedCharacter"
  :stars="stars"
  @select="onSelectCharacter"
/>
```

Do NOT modify any other line in App.vue. Do NOT introduce a computed wrapper — `stars` is already a reactive ref from `useMathGame` and Vue unwraps it correctly in the template.

`stars` is already destructured at the top of `<script setup>` (line ~27: `const { stars, streak, problemKey, ... } = useMathGame()`), so no new imports or declarations are needed.
  </action>
  <verify>
    <automated>npm run test</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep ":stars=\"stars\"" src/App.vue` matches at least 1 line in the CharacterSelect block (file may already use :stars elsewhere for other components — verify the new occurrence is inside the `<CharacterSelect ...>` element by checking surrounding context)
    - `grep -A 3 "<CharacterSelect" src/App.vue` shows `:stars="stars"` between the opening tag and closing `/>`
    - No new `import` lines added
    - No changes to useMathGame destructuring line
    - `npm run test` passes (existing 27 tests)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>App.vue passes the reactive `stars` ref to CharacterSelect; unlock state in the component reacts live to star changes; build and tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify Phase 8 end-to-end</name>
  <read_first>
    - .planning/phases/08-more-mascots/08-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <what-built>
    - CharacterSelect.vue: 10 characters (peach, daisy, rosalina, toad, yoshi, luigi, bowser, birdo, toadette, boo)
    - Locked entries (stars below unlockAt) render greyed-out with a ⭐ {N} cost badge
    - Grid layout: 2 cols on mobile, 5 cols on desktop (2 even rows of 5)
    - useSound.js: playThemeMusic falls back to title theme for new char ids
    - App.vue: passes live `stars` ref into CharacterSelect via `:stars`
  </what-built>
  <how-to-verify>
1. **Build and run dev server**:
   - `npm run dev`
   - Open the printed localhost URL in a desktop browser at ≥768px width

2. **Test locked state (stars=0)**:
   - Open DevTools → Application → Local Storage → clear `emma-stars` (or set to `0`)
   - Hard-refresh the page
   - On the character select screen, confirm:
     - All 10 characters are visible
     - Original 4 (Peach, Daisy, Rosalina, Toad) are full-color and clickable
     - New 6 (Yoshi, Luigi, Bowser, Birdo, Toadette, Boo) are greyed-out (opacity-40 grayscale)
     - Each locked character shows a `⭐ {threshold}` badge in the top-right corner
     - Expected thresholds: Yoshi=25, Luigi=50, Bowser=75, Birdo=100, Toadette=150, Boo=200
     - Clicking a locked character does NOTHING (no selection, no music)
     - Grid renders as 2 rows of 5 on desktop, 5 rows of 2 on mobile (resize browser to confirm)

3. **Test partial unlock (stars=75)**:
   - In DevTools console: `localStorage.setItem('emma-stars', '75'); location.reload()`
   - Confirm: Yoshi, Luigi, Bowser are now selectable (no grey, no badge)
   - Confirm: Birdo, Toadette, Boo are still locked with badges

4. **Test full unlock (stars=200)**:
   - In DevTools console: `localStorage.setItem('emma-stars', '200'); location.reload()`
   - Confirm: All 10 characters are selectable
   - Click Yoshi → game starts, no console errors, title theme music plays (since yoshi has no MP3)
   - Return to character select (clear `emma-selectedCharacter` or use app's reset path) and try Luigi, Bowser, Birdo, Toadette, Boo — each should start the game and play title theme without errors

5. **Test no new localStorage keys**:
   - Inspect DevTools → Application → Local Storage
   - Confirm only existing `emma-*` keys are present (emma-stars, emma-level, emma-mute, emma-volume, emma-pwa-dismissed, etc.)
   - Confirm NO new key like `emma-unlocked-characters` or per-character unlock keys exist
  </how-to-verify>
  <acceptance_criteria>
    - Locked characters render greyed with ⭐ {N} badges at exactly 25/50/75/100/150/200
    - Grid is 2-col mobile / 5-col desktop with 2 even rows on desktop
    - All 6 new characters can be selected at stars=200 without console errors
    - Title theme plays when selecting any of the 6 new characters
    - No new localStorage keys are introduced
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark Phase 8 complete, or describe any issues found.</resume-signal>
  <done>Phase 8 visually and functionally verified end-to-end; ready to close phase in ROADMAP.</done>
</task>

</tasks>

<threat_model>
Per Plan 01: no new security surface introduced. Same disposition stands.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | localStorage emma-stars (wired into UI gating) | accept | Same rationale as T-08-01: cosmetic-only client-side gate, no server reward |
</threat_model>

<verification>
- `npm run test` passes
- `npm run build` passes
- Human-verify checkpoint approved
</verification>

<success_criteria>
1. App.vue passes `:stars="stars"` to `<CharacterSelect>`
2. Locked characters react live: gaining stars during a session that returns to character select shows newly-unlocked entries without refresh
3. All 6 new characters playable at stars=200, no console errors, title theme fallback works
4. No new localStorage keys
5. Human-verify checkpoint receives "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/08-more-mascots/08-02-SUMMARY.md`.
</output>
