---
phase: 02-multiplication-division
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - src/composables/__tests__/useMathGame.test.js
autonomous: true
requirements: []
nyquist_compliant: true
tags: [testing, vitest, wave0]

must_haves:
  truths:
    - "npx vitest run exits 0 with the scaffold test suite"
    - "vitest and jsdom are listed in package.json devDependencies"
    - "src/composables/__tests__/useMathGame.test.js exists and imports useMathGame without error"
  artifacts:
    - path: "package.json"
      provides: "Test framework dependencies + test script"
      contains: "\"test\": \"vitest run\""
    - path: "vite.config.js"
      provides: "Vitest jsdom environment configuration"
      contains: "environment: 'jsdom'"
    - path: "src/composables/__tests__/useMathGame.test.js"
      provides: "Scaffold test file with MATH-01/MATH-02/MATH-03/D-08/D-14 placeholder tests"
      contains: "describe('useMathGame'"
  key_links:
    - from: "package.json"
      to: "vitest"
      via: "devDependencies"
      pattern: "\"vitest\""
    - from: "vite.config.js"
      to: "vitest runtime"
      via: "test.environment config"
      pattern: "environment:\\s*'jsdom'"
---

<objective>
Install test infrastructure (vitest + @vue/test-utils + jsdom) and create the scaffold test file that later plans' tasks will fill in. No production code changes in this plan — this is Wave 0 per VALIDATION.md.

Purpose: Every later task depends on `npx vitest run` working. The test file must exist as an importable scaffold before Plan 02 can add real assertions.
Output: Working `npx vitest run` command, scaffold test file with failing placeholder tests for MATH-01, MATH-02, MATH-03, D-08, D-14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multiplication-division/02-VALIDATION.md
@.planning/phases/02-multiplication-division/02-RESEARCH.md
@package.json
@vite.config.js
@src/composables/useMathGame.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install vitest + jsdom and add test script</name>
  <files>package.json</files>
  <read_first>
    - package.json (confirm current devDependencies, no vitest yet)
    - .planning/phases/02-multiplication-division/02-VALIDATION.md (confirm framework: vitest)
  </read_first>
  <action>
    Run exactly: `npm install --save-dev vitest@^3 @vue/test-utils@^2 jsdom@^25`
    Then add a `"test": "vitest run"` entry to the `scripts` object in package.json. Final scripts block must contain:
      "dev": "vite",
      "build": "vite build",
      "preview": "vite preview",
      "test": "vitest run"
    Do NOT add "test:watch" or "test:coverage" — only the single `test` script. Do NOT run `npm audit fix`. Do NOT change existing dependency versions.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.devDependencies.vitest) process.exit(1); if(!p.devDependencies.jsdom) process.exit(1); if(!p.devDependencies['@vue/test-utils']) process.exit(1); if(p.scripts.test !== 'vitest run') process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"vitest"\\s*:' package.json` matches
    - `grep -E '"jsdom"\\s*:' package.json` matches
    - `grep -E '"@vue/test-utils"\\s*:' package.json` matches
    - `grep -E '"test":\\s*"vitest run"' package.json` matches
    - node_modules/vitest/package.json exists
    - `npm run build` still exits 0 (no regression)
  </acceptance_criteria>
  <done>devDependencies contain vitest, @vue/test-utils, jsdom; package.json scripts contain `"test": "vitest run"`; `npm run build` still passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure vite.config.js test.environment = 'jsdom'</name>
  <files>vite.config.js</files>
  <read_first>
    - vite.config.js (see current plugin/alias config before adding `test` block)
  </read_first>
  <action>
    Edit vite.config.js so the exported defineConfig object includes a `test` key:
    ```js
    test: {
      environment: 'jsdom',
      globals: false,
      include: ['src/**/*.test.js', 'src/**/__tests__/**/*.test.js'],
    }
    ```
    Preserve all existing plugins, aliases, and resolve config. Do NOT switch to a separate `vitest.config.js` file — vitest reads from vite.config.js by design. Do NOT enable globals (keep `globals: false` so tests must `import { describe, it, expect } from 'vitest'`).
    If the file uses `defineConfig(() => ({...}))` functional form, add the `test` key inside the returned object. If it uses `defineConfig({...})` object form, add it as a top-level key.
  </action>
  <verify>
    <automated>node -e "import('./vite.config.js').then(m => { const cfg = typeof m.default === 'function' ? m.default({mode:'test',command:'serve'}) : m.default; if (!cfg.test || cfg.test.environment !== 'jsdom') { console.error('bad config', JSON.stringify(cfg.test)); process.exit(1); } console.log('ok'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "environment:\\s*'jsdom'" vite.config.js` matches exactly once
    - `grep -n "include:\\s*\\[" vite.config.js` matches (include array present)
    - `grep -n "test:\\s*{" vite.config.js` matches exactly once
    - `npm run build` still exits 0
    - `npx vitest --version` prints a version number without error
  </acceptance_criteria>
  <done>vite.config.js exports a config whose `test.environment` equals `'jsdom'` and whose `test.include` covers the `__tests__` directory.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create scaffold test file with placeholder tests for Phase 2 requirements</name>
  <files>src/composables/__tests__/useMathGame.test.js</files>
  <read_first>
    - src/composables/useMathGame.js (confirm the `useMathGame` named export exists)
    - .planning/phases/02-multiplication-division/02-VALIDATION.md (Per-Task Verification Map table)
    - .planning/phases/02-multiplication-division/02-RESEARCH.md (Phase Requirements → Test Map)
  </read_first>
  <action>
    Create the file `src/composables/__tests__/useMathGame.test.js`. It MUST:

    1. Import vitest primitives explicitly (globals are disabled):
       ```js
       import { describe, it, expect, beforeEach } from 'vitest'
       import { useMathGame } from '../useMathGame.js'
       ```

    2. Contain a `beforeEach` that clears localStorage (to isolate tests):
       ```js
       beforeEach(() => { localStorage.clear() })
       ```

    3. Contain the following `describe('useMathGame', () => { ... })` structure with placeholder tests. Every placeholder MUST call `expect.fail('Wave 1 implementation pending — MATH-XX')` so the suite is RED until Plan 02 adds real assertions. Use this exact set of test names (they are referenced by later plans):
       - `it('MATH-01: multiplication operator only appears when level >= 3', ...)` → expect.fail
       - `it('MATH-02: division operator only appears when level >= 5', ...)` → expect.fail
       - `it('MATH-02: division always produces a whole-number answer (no remainder)', ...)` → expect.fail
       - `it('MATH-03: maxOperandByOperator is an object with per-operator values', ...)` → expect.fail
       - `it('MATH-03: × and ÷ cap maxOperand at 10; + caps at 20', ...)` → expect.fail
       - `it('D-08: divisor is never 0 and quotient is never 0', ...)` → expect.fail
       - `it('D-14: tutorial flag prevents re-firing after dismissal', ...)` → expect.fail

    4. Add a single passing smoke test to confirm the import works and vitest is wired correctly:
       ```js
       it('smoke: useMathGame returns an object with a generateProblem function', () => {
         const game = useMathGame()
         expect(typeof game.generateProblem).toBe('function')
       })
       ```

    AVOID: Writing real MATH-* assertions in this plan. Those belong to Plan 02 Task 4. This task only produces the scaffold + smoke.
    AVOID: Using `vi.fn()` or mocks — none needed for scaffold.
    AVOID: Importing `@vue/test-utils` here — this is a composable test, not a component test.
  </action>
  <verify>
    <automated>npx vitest run src/composables/__tests__/useMathGame.test.js --reporter=verbose 2>&1 | tee /tmp/phase2-wave0.log; grep -E "Tests\\s+[0-9]+ failed.*[0-9]+ passed" /tmp/phase2-wave0.log && grep -E "1 passed" /tmp/phase2-wave0.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/composables/__tests__/useMathGame.test.js`
    - `grep -c "it('MATH-" src/composables/__tests__/useMathGame.test.js` outputs a number ≥ 5
    - `grep -c "it('D-" src/composables/__tests__/useMathGame.test.js` outputs a number ≥ 2
    - `grep -c "expect.fail" src/composables/__tests__/useMathGame.test.js` outputs a number ≥ 7
    - `grep -n "smoke: useMathGame returns an object with a generateProblem function" src/composables/__tests__/useMathGame.test.js` matches
    - `npx vitest run src/composables/__tests__/useMathGame.test.js` exits non-zero (expected: placeholder fails are intentional) AND prints "1 passed" for the smoke test
  </acceptance_criteria>
  <done>Scaffold file exists with 7 failing placeholder tests + 1 passing smoke test. `npx vitest run` runs to completion (not a config error).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 2 introduces no new trust boundaries. Wave 0 installs developer-only tooling (vitest/jsdom) that never ships to production (devDependencies). The test file reads from `localStorage` in a jsdom sandbox — no production data, no network calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Supply chain (vitest/jsdom transitive deps) | accept | Pinned major versions (`^3`, `^25`); `npm audit` is run manually before shipping; no prod-facing surface (devDependencies only) |
| T-02-02 | Information Disclosure | Test file logs leaking production data | accept | Test file reads only localStorage, which is empty in jsdom sandbox. No API keys, no PII. |
</threat_model>

<verification>
- [ ] `npm install` completes without errors
- [ ] `npx vitest --version` prints a version
- [ ] `npx vitest run src/composables/__tests__/useMathGame.test.js` runs to completion (exit code irrelevant — placeholders fail by design)
- [ ] Smoke test passes (grep log for "1 passed")
- [ ] `npm run build` still exits 0
</verification>

<success_criteria>
- [ ] vitest, @vue/test-utils, jsdom in devDependencies
- [ ] `test` script present: `"test": "vitest run"`
- [ ] `vite.config.js` has `test.environment: 'jsdom'`
- [ ] `src/composables/__tests__/useMathGame.test.js` exists with 7 failing placeholders + 1 smoke test
- [ ] Plan 02 can import the scaffold file and fill in real assertions
</success_criteria>

<output>
After completion, create `.planning/phases/02-multiplication-division/02-01-SUMMARY.md` with: framework chosen, exact versions installed, any deviations from VALIDATION.md sampling plan, and the exact command future waves should run.
</output>
