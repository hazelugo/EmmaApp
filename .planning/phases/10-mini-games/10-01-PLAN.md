---
phase: 10
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/composables/__tests__/useMiniGame.test.js
autonomous: true
requirements:
  - MINI-01
must_haves:
  truths:
    - "Test file exists at src/composables/__tests__/useMiniGame.test.js and runs (red)"
    - "Tests describe trigger threshold, random 5-7 range, result callback, and resetMiniGame"
    - "All tests fail with module-not-found or unimplemented errors (no useMiniGame.js exists yet)"
  artifacts:
    - path: "src/composables/__tests__/useMiniGame.test.js"
      provides: "Failing unit tests for useMiniGame composable"
      min_lines: 80
      contains: "describe.*useMiniGame"
  key_links:
    - from: "src/composables/__tests__/useMiniGame.test.js"
      to: "src/composables/useMiniGame.js"
      via: "import { useMiniGame } from '../useMiniGame.js'"
      pattern: "import .* from .*useMiniGame"
---

<objective>
Wave 0: Create failing unit tests for `useMiniGame` composable covering MINI-01a/b/c/d/f/g per VALIDATION.md.

Purpose: Lock the trigger contract (absolute threshold, not modulo) and result lifecycle in tests BEFORE implementation. Per D-01, threshold must be an absolute value reset to `streak + random(5,7)` after each activation — modulo is explicitly forbidden because streak milestones at 5/10/15 already trigger confetti.

Output: `src/composables/__tests__/useMiniGame.test.js` with ~6 failing tests. No implementation yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-mini-games/10-RESEARCH.md
@.planning/phases/10-mini-games/10-VALIDATION.md
@src/composables/__tests__/useMathGame.test.js
@src/composables/useTimer.js

<interfaces>
<!-- Existing test setup conventions in the project -->

From src/composables/__tests__/useMathGame.test.js (existing pattern):
- Uses `import { describe, it, expect, beforeEach, vi } from 'vitest'`
- Calls composable directly (no @vue/test-utils): `const game = useMathGame()`
- Mocks Math.random when needed via `vi.spyOn(Math, 'random')`
- Tests on .value of returned refs

Contract to be defined (target, does not exist yet):
```javascript
// src/composables/useMiniGame.js — to be created in Plan 02
export function useMiniGame() {
  // refs:
  //   showMiniGame: Ref<boolean>
  //   activeGame: Ref<'math-dash' | 'shape-match' | 'jump-sequence' | null>
  //   miniGameResult: Ref<{ won: boolean, bonus: number } | null>
  // methods:
  //   checkMiniGameTrigger(streak: number, showLevelVictory?: boolean): boolean
  //   onMiniGameResult(result: { won: boolean, bonus: number }): void
  //   resetMiniGame(): void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for useMiniGame composable</name>
  <files>src/composables/__tests__/useMiniGame.test.js</files>
  <read_first>
    - src/composables/__tests__/useMathGame.test.js (test style + imports)
    - src/composables/useTimer.js (composable shape being mirrored)
    - .planning/phases/10-mini-games/10-RESEARCH.md sections "Code Examples" and "Phase Requirements → Test Map"
    - .planning/phases/10-mini-games/10-VALIDATION.md "Wave 0 Requirements"
  </read_first>
  <behavior>
    The test file MUST contain the following test cases (one `it()` block each):

    1. MINI-01a — "does not trigger when streak is below threshold":
       - Mock Math.random to return 0 (so initial threshold = 5).
       - Call `checkMiniGameTrigger(4)`. Expect return false, `showMiniGame.value === false`, `activeGame.value === null`.

    2. MINI-01a — "triggers when streak reaches threshold":
       - Mock Math.random to return 0 (initial threshold = 5; activeGame selection = 'math-dash').
       - Call `checkMiniGameTrigger(5)`. Expect return true, `showMiniGame.value === true`, `activeGame.value` in MINI_GAME_TYPES, `miniGameResult.value === null`.

    3. MINI-01c — "threshold randomizes within 5–7 range":
       - Run 100 iterations: fresh composable, mock Math.random returning sequential values [0, 0.34, 0.67, 0.99]. Trigger at threshold each time. Collect the threshold values used.
       - Expect every observed threshold to be in {5, 6, 7}. Expect at least 2 distinct values across runs (verifies randomization is wired).

    4. MINI-01a — "uses absolute threshold, not modulo (regression for D-01)":
       - Mock Math.random so threshold = 5 first call, then 6 after reset.
       - Call `checkMiniGameTrigger(5)` → triggers. Call `onMiniGameResult({ won: true, bonus: 2 })`.
       - Call `checkMiniGameTrigger(10)` → should NOT trigger (modulo-5 would trigger here). Call `checkMiniGameTrigger(11)` → SHOULD trigger (5 + 6 = 11). Expect this exact behavior.

    5. MINI-01d — "onMiniGameResult clears overlay state and stores result":
       - Trigger first (streak=5). Then call `onMiniGameResult({ won: true, bonus: 3 })`.
       - Expect `showMiniGame.value === false`, `activeGame.value === null`, `miniGameResult.value` equals `{ won: true, bonus: 3 }`.

    6. MINI-01f — "all 3 mini-game types are selectable":
       - Mock Math.random to return 0, then 0.5, then 0.99 across 3 separate triggers (resetting result between each).
       - Collect activeGame values. Expect set to include all of: 'math-dash', 'shape-match', 'jump-sequence'.

    7. MINI-01g — "resetMiniGame clears all state and resets threshold":
       - Trigger a mini-game (streak=5). Call `resetMiniGame()`.
       - Expect `showMiniGame.value === false`, `activeGame.value === null`, `miniGameResult.value === null`.
       - Mock Math.random fresh, then call `checkMiniGameTrigger(5)` (NOT streak+5+random — after reset, threshold should be a fresh 5-7 starting from 0). Expect it to trigger again at 5.

    8. (Optional but recommended) — "checkMiniGameTrigger skips when showLevelVictory is true":
       - Mock Math.random → threshold=5. Call `checkMiniGameTrigger(5, true)`. Expect return false, no state change. (Implements Pitfall 5 from RESEARCH.md.)
  </behavior>
  <action>
    Create the test file at `src/composables/__tests__/useMiniGame.test.js`.

    Use the existing useMathGame.test.js as the structural template:
    ```javascript
    import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'
    import { useMiniGame } from '../useMiniGame.js'
    ```

    Wrap all tests in a top-level `describe('useMiniGame', () => { ... })`.

    For each test that needs deterministic randomness, use `vi.spyOn(Math, 'random').mockReturnValue(X)` inside the test, and call `vi.restoreAllMocks()` in `afterEach`.

    The MINI_GAME_TYPES set the tests assert against is exactly: `['math-dash', 'shape-match', 'jump-sequence']`.

    Do NOT create `src/composables/useMiniGame.js`. The import will fail — that is the expected RED state.

    Add a top-of-file comment: `// Wave 0 RED tests for MINI-01. Implementation lands in Plan 02. Per D-01: absolute threshold, NEVER modulo.`
  </action>
  <verify>
    <automated>npm run test -- --run useMiniGame 2>&1 | tee /tmp/wave0.log; grep -E "(Cannot find|failed|FAIL)" /tmp/wave0.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/composables/__tests__/useMiniGame.test.js`
    - Contains at least 6 `it(` blocks: `grep -c "it(" src/composables/__tests__/useMiniGame.test.js` returns >= 6
    - Imports from `../useMiniGame.js`: `grep "from '../useMiniGame.js'" src/composables/__tests__/useMiniGame.test.js`
    - Contains regression test for D-01 absolute threshold: `grep -E "modulo|absolute threshold|D-01" src/composables/__tests__/useMiniGame.test.js`
    - Asserts all 3 mini-game types: `grep -E "math-dash.*shape-match.*jump-sequence|jump-sequence.*shape-match" src/composables/__tests__/useMiniGame.test.js` (any ordering)
    - `npm run test -- --run useMiniGame` exits non-zero (RED state expected — module does not exist yet)
  </acceptance_criteria>
  <done>
    Test file exists and runs. All tests fail because useMiniGame.js does not exist. No production code created in this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure test scaffolding; no input crosses any trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | test fixture | accept | Tests only — no production code path. ASVS L1 N/A for test files. |
</threat_model>

<verification>
- `npm run test -- --run useMiniGame` produces a failing result with "Cannot find module" or similar — this confirms RED state.
- No other test files modified.
- No production source files created.
</verification>

<success_criteria>
- Test file committed, fails as expected, ready for Plan 02 to drive to GREEN.
- Tests encode D-01 absolute-threshold semantics so a future modulo regression breaks immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/10-mini-games/10-01-SUMMARY.md` describing what tests were added and their expected RED state.
</output>
