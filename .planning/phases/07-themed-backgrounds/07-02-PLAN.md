---
phase: 07-themed-backgrounds
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/App.vue
  - src/style.css
autonomous: false
requirements:
  - THEME-01
must_haves:
  truths:
    - "The .difficulty-bg div is rendered as the first child inside #app-root in App.vue"
    - "Its class is bound to a computed that derives a theme name from difficulty.maxOperandByOperator using pickMaxDifficultyTheme"
    - "Crossing a maxOperand threshold during gameplay swaps the class, triggering an 800ms CSS fade"
    - "The body element no longer paints its own gradient (body { background: none }) so the new themed layer is the only background"
    - "body::before / body::after cloud decorations still render unchanged"
    - "All 4 themes appear visually distinct when difficulty is manually advanced"
    - "Existing tests still pass after wiring"
  artifacts:
    - path: "src/App.vue"
      provides: ".difficulty-bg layer + difficultyThemeClass computed wired to useMathGame difficulty"
      contains: "difficulty-bg"
    - path: "src/style.css"
      provides: "Neutralized body background (background: none) so .difficulty-bg owns the page"
      contains: "background: none"
  key_links:
    - from: "src/App.vue"
      to: "src/utils/difficultyTheme.js"
      via: "import { pickMaxDifficultyTheme }"
      pattern: "from ['\"]\\./utils/difficultyTheme"
    - from: "src/App.vue computed difficultyThemeClass"
      to: "useMathGame difficulty.maxOperandByOperator"
      via: "computed reactive read"
      pattern: "difficulty\\.maxOperandByOperator"
    - from: "src/App.vue template .difficulty-bg div"
      to: "difficultyThemeClass computed"
      via: ":class binding"
      pattern: ":class=\"difficultyThemeClass\""
---

<objective>
Wire the difficulty-driven background system into the live app: render the `.difficulty-bg` layer inside App.vue, bind it reactively to a computed derived from `difficulty.maxOperandByOperator`, and neutralize the existing `body` gradient so the new themed layer owns the viewport background. End with a human-verify checkpoint to confirm the 800ms transition and 4 visually distinct themes.

Purpose: Delivers THEME-01 end-to-end. Plan 01 created the primitives in isolation; this plan connects them.

Output: Updated `src/App.vue` (import + computed + template div) and `src/style.css` (body background neutralized). Visually verified in a browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-themed-backgrounds/07-RESEARCH.md
@.planning/phases/07-themed-backgrounds/07-VALIDATION.md
@.planning/phases/07-themed-backgrounds/07-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@src/App.vue
@src/style.css

<interfaces>
<!-- From Plan 01 (now committed) -->

src/utils/difficultyTheme.js exports:
```javascript
export function pickDifficultyTheme(maxOperand: number): string
export function pickMaxDifficultyTheme(maxOperandByOperator: Record<string, number>): string
```

src/style.css contains: .difficulty-bg, .difficulty-bg.theme-overworld, .theme-underground, .theme-desert, .theme-space

<!-- From useMathGame.js (already destructured in App.vue line 29) -->
- `difficulty` is a reactive() object exposing `difficulty.maxOperandByOperator` (object keyed by '+', '-', '×', '÷')

<!-- Existing template entry point in App.vue lines 403-406 -->
<div
  id="app-root"
  class="relative z-10 flex flex-col min-h-dvh max-w-lg mx-auto px-3 py-3 gap-3 select-none"
>
  ... content ...
</div>

<!-- Existing class-binding pattern (App.vue line 397-400) -->
<div
  v-if="streakFlash"
  class="fixed inset-0 pointer-events-none z-[500]"
  :class="streakFlash"
/>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import utility, add difficultyThemeClass computed, render .difficulty-bg div in App.vue</name>
  <files>src/App.vue</files>
  <read_first>
    - src/App.vue (full file — must see line 15 `import { ref, computed, watch, onMounted, onBeforeUnmount } from 'vue'` to confirm computed is already imported; line 18-23 for composable import block; lines 25-38 for useMathGame destructure with `difficulty`; lines 390-510 for template head)
    - src/utils/difficultyTheme.js (to confirm export name `pickMaxDifficultyTheme`)
    - .planning/codebase/CONVENTIONS.md (relative import paths, 2-space indent, single quotes)
  </read_first>
  <action>
Make THREE precise edits to `src/App.vue`:

**Edit 1 — Add import** (in the composable/util import block around lines 18-23, after the existing composable imports, before `/* ── Composables ──────────...`):

Add a new line:
```javascript
import { pickMaxDifficultyTheme } from './utils/difficultyTheme.js'
```

Place it after the last `import { useX } from './composables/...'` line. Use single quotes, no semicolon (matching file style — check existing import lines: existing imports also use no trailing semicolons in this file based on the surrounding context; if surrounding imports DO have semicolons, match that).

**Edit 2 — Add computed** (in `<script setup>`, after the `useMathGame` destructure block ending around line 38, and after `const timer = useTimer()` on line 40, but BEFORE the existing `/* ── Timer Mode State ───`... section divider on line 42). Insert this block with its own section divider:

```javascript
/* ── Difficulty-Driven Background Theme ────────────────────────── */
const difficultyThemeClass = computed(
  () => pickMaxDifficultyTheme(difficulty.maxOperandByOperator)
)
```

`computed` is already imported on line 15. `difficulty` is already destructured on line 29.

**Edit 3 — Render the background div** (in `<template>`, inside `#app-root`, as the FIRST CHILD — BEFORE the existing `<!-- Character Select Overlay -->` Transition block at line 407). The div must be inside `#app-root` so it lives within the Vue-managed tree, but it uses `position: fixed` (from the CSS class) so it visually fills the viewport.

Insert exactly this, immediately after the opening `<div id="app-root" ...>` tag (after line 406, before line 407):

```html
    <!-- Phase 7: Difficulty-driven themed background (fixed, z-index:-1) -->
    <div
      class="difficulty-bg"
      :class="difficultyThemeClass"
      aria-hidden="true"
    />
```

Use 4-space indentation to match the surrounding template indentation level. Self-closing tag is fine (Vue 3 SFC compiler accepts it for non-void elements; existing patterns in this file use both styles — match the nearest neighbor).

DO NOT modify the existing `streakFlash` Transition block (lines 394-401) — it lives outside `#app-root` and is correct as-is.

DO NOT touch any other component imports, composable destructuring, or template content.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from './utils/difficultyTheme" src/App.vue` returns a match
    - `grep -n "pickMaxDifficultyTheme" src/App.vue` returns at least 2 matches (import + usage)
    - `grep -n "const difficultyThemeClass = computed" src/App.vue` returns a match
    - `grep -n "difficulty.maxOperandByOperator" src/App.vue` returns at least one match (in the computed)
    - `grep -n "class=\"difficulty-bg\"" src/App.vue` returns a match
    - `grep -n ":class=\"difficultyThemeClass\"" src/App.vue` returns a match
    - `grep -n "aria-hidden=\"true\"" src/App.vue` returns at least one match (the new bg div; may be more if already present elsewhere)
    - The `.difficulty-bg` div appears AFTER `id="app-root"` and BEFORE `<!-- Character Select Overlay -->`: verified by line-number ordering in `grep -n "app-root\\|difficulty-bg\\|Character Select Overlay" src/App.vue`
    - `npm run build` exits 0
    - `npm run test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    App.vue imports pickMaxDifficultyTheme, exposes a difficultyThemeClass computed bound to difficulty.maxOperandByOperator, and renders a .difficulty-bg div as the first child of #app-root with reactive :class binding.
  </done>
</task>

<task type="auto">
  <name>Task 2: Neutralize body gradient in style.css so .difficulty-bg owns the background</name>
  <files>src/style.css</files>
  <read_first>
    - src/style.css (full file — must see the existing `body { background: linear-gradient(...) }` block at lines ~80-92 AND the `body::before` / `body::after` cloud rules at lines ~94-121, plus the `@theme {}` token block and reduced-motion block)
    - .planning/phases/07-themed-backgrounds/07-RESEARCH.md (Common Pitfalls #2 — body override conflict, and the "Body Cleanup" code example)
  </read_first>
  <action>
Edit `src/style.css` line ~80-92 only. Replace the existing `body { background: linear-gradient(...) }` rule with:

```css
body {
  /* Phase 7: Themed background is owned by .difficulty-bg in App.vue.
     body gradient neutralized so it doesn't bleed through behind the
     fixed themed layer (which uses z-index: -1).
     body::before / body::after cloud decorations are unaffected. */
  background: none;
}
```

CRITICAL — preserve everything else in the body rule. If the existing `body { ... }` rule contains OTHER properties besides `background:` (e.g., `font-family`, `color`, `margin`, `min-height`), KEEP THEM. Only the `background: linear-gradient(...)` value should change to `background: none;`. The exact replacement target is the multi-line `background: linear-gradient(180deg, var(--color-sky-top) 0%, var(--color-sky) 40%, var(--color-luigi-light) 75%, var(--color-ground-brown) 100%);` declaration.

DO NOT touch:
- `body::before` (cloud decoration, lines ~94-105)
- `body::after` (cloud decoration, lines ~106-121)
- `@theme {}` color tokens
- The `*, *::before, *::after { box-sizing }` reset
- The `@media (prefers-reduced-motion: reduce)` block

Verification expectation: the page should still show clouds floating, and the `.difficulty-bg` div (added in Task 1) should now provide the full-viewport gradient with smooth 800ms transitions when difficulty crosses thresholds.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nA1 "^body {" src/style.css` shows the body rule containing `background: none` (NOT `background: linear-gradient`)
    - `grep -c "body::before" src/style.css` returns at least 1
    - `grep -c "body::after" src/style.css` returns at least 1
    - `grep -c "background: linear-gradient(180deg" src/style.css` returns at least 5 (the 4 theme variants + 1 .difficulty-bg base from Plan 01; body's gradient is gone)
    - `grep -n "Phase 7" src/style.css` returns at least one match (the explanatory comment in body or the section header from Plan 01)
    - `npm run build` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>
    body { background: none } replaces the old gradient; clouds and reduced-motion rules untouched; build + tests still pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — 4 themes, 800ms transition, no visual regressions</name>
  <what-built>
    Difficulty-driven themed backgrounds (THEME-01): a fixed `.difficulty-bg` layer inside App.vue whose CSS class is reactively bound to `pickMaxDifficultyTheme(difficulty.maxOperandByOperator)`. The body gradient has been neutralized; clouds still float. Four themes (overworld / underground / desert / space) map to max-operand ranges (≤7, ≤12, ≤17, >17) and transition via CSS over 800ms.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in Chrome/Edge desktop.
    2. **Theme 1 — Overworld (default):** On fresh load with cleared localStorage, the background should be the blue-sky/green-ground gradient (max ≤ 7). Confirm clouds still float at the top.
    3. **Theme transitions:** Open DevTools → Application → Local Storage → clear `emma-*` keys. Reload. Then in the JS console, manually bump difficulty by running:
       ```js
       // Simulate progression — find the running app's difficulty and bump it
       // Or just play through ~20-30 correct answers to climb difficulty naturally
       ```
       Easier path: solve problems quickly (correct answers increase maxOperand). Watch for the background to smoothly fade to **underground (purple)** around maxOperand 8, **desert (orange)** around 13, **space (navy)** around 18.
    4. **800ms timing:** Each transition should be visibly smooth (~0.8s), NOT a snap. If it snaps instantly, gradient interpolation may have failed (see RESEARCH.md Pitfall 1 — gradient stop counts must match across themes; they do, so this should work).
    5. **Visual distinctness:** All 4 themes should be obviously different at a glance. Take a screenshot of each.
    6. **No regressions:**
       - Clouds (☁️ emoji or pseudo-element clouds) still visible at top of screen.
       - All overlays still render correctly on top (CharacterSelect, LevelUpModal, ShopOverlay, SoundSettings).
       - Streak flash effect (gold/star flash) still works at streak milestones.
    7. **Reduced motion check (optional):** Enable `prefers-reduced-motion` in OS settings (or DevTools → Rendering → Emulate CSS media feature → reduce). Reload. Background should snap (no animation) — the global reduced-motion rule at style.css ~310-323 handles this automatically.
    8. **Mobile/PWA check:** Open in mobile device or DevTools mobile view; confirm `.difficulty-bg` still fills the viewport (no white edges from `position: fixed` + `inset: 0`).
    9. **Safari (if available):** Test on iOS Safari — gradient interpolation is the most browser-sensitive part of this phase. If Safari snaps instead of fading, log it as a follow-up (would need opacity-layer workaround per RESEARCH.md A1).
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 themes are distinct, transitions are smooth ~800ms, clouds and overlays still work, no console errors. Otherwise describe what you saw and I'll investigate.</resume-signal>
</task>

</tasks>

<verification>
- `npm run test` passes (all existing tests including new difficultyTheme tests)
- `npm run build` passes
- Visual verification in browser confirms 4 distinct themes + 800ms transitions
- No regressions to overlays, clouds, or streak flash
- THEME-01 requirement fully satisfied
</verification>

<success_criteria>
- App.vue wires .difficulty-bg layer with reactive class binding to difficulty.maxOperandByOperator
- body gradient neutralized; .difficulty-bg owns the viewport background
- All 4 themes visually distinct and smoothly transition over 800ms
- Reduced-motion preference still respected via existing global rule
- Build + tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-themed-backgrounds/07-02-SUMMARY.md` documenting:
- Exact lines modified in App.vue and style.css
- Browser(s) tested + any gradient-interpolation observations
- Screenshots noted (paths if saved)
- Any follow-ups (e.g., Safari snap → opacity workaround needed)
- Confirmation that THEME-01 acceptance criteria are met
</output>
