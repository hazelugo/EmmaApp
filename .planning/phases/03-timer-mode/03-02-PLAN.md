---
phase: 3
plan: 2
wave: 2
---

# Plan 3.2: Timer UI Components

## Objective
Create the Results overlay for when the Timer sprint ends, and add the Sprint button to the `ScoreHeader`. Add an active timer visual to `ScoreHeader`.

## Context
- .planning/phases/03-timer-mode/03-CONTEXT.md
- src/components/TimerResultsOverlay.vue (NEW)
- src/components/ScoreHeader.vue

## Tasks

<task type="auto">
  <name>Create TimerResultsOverlay</name>
  <files>
    src/components/TimerResultsOverlay.vue
  </files>
  <action>
    - Create `TimerResultsOverlay.vue` following the pattern in `ShopOverlay.vue` (uses `<Transition name="fade">`).
    - Props: `show` (Boolean), `coins` (Number), `correctCount` (Number), `highScore` (Number), `isNewHighScore` (Boolean).
    - Emits: `['close']`.
    - UI: Prominent "Time's Up!" heading, stat counters for correct problems solved and coins earned. If `isNewHighScore` is true, show a celebratory "New High Score!" badge. A "Done" button to close the overlay.
  </action>
  <verify>npm run build</verify>
  <done>TimerResultsOverlay is complete and ready for integration.</done>
</task>

<task type="auto">
  <name>Add Sprint button and Timer Display to ScoreHeader</name>
  <files>
    src/components/ScoreHeader.vue
  </files>
  <action>
    - Props to add: `isTimerMode` (Boolean), `timeLeft` (Number).
    - Emits to add: `start-sprint`.
    - Modification: 
      - If `isTimerMode` is false, show a new round button beside Shop and Mute that says "⏱ Sprint", which emits `start-sprint`.
      - If `isTimerMode` is true, replace the title "Emma's Star World" with a bold countdown (e.g. `⏳ {{ timeLeft }}s`), and maybe give it dynamic coloring (red when `< 10s`).
  </action>
  <verify>npm run build</verify>
  <done>ScoreHeader contains the Sprint start button and shows the countdown when active.</done>
</task>

## Success Criteria
- [ ] `TimerResultsOverlay.vue` is styled appropriately for the game's aesthetic.
- [ ] `ScoreHeader.vue` has a Sprint button that emits `start-sprint`.
- [ ] When `isTimerMode` is true, the `ScoreHeader.vue` shows the timer countdown.
