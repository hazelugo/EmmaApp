# Architecture

> Auto-generated by /map on 2026-04-12

## Overview

A Vue 3 web application using Vite and Tailwind CSS. It appears to be an interactive kids math game ("EmmaApp"), utilizing Vue components for UI elements like character selection, a number pad, score header, and challenge zone. The game logic and sound are encapsulated in Vue composables (`useMathGame.js` and `useSound.js`).

```
┌─────────────────────────────────────────┐
│              App.vue                    │
├─────────────────────────────────────────┤
│         Composables (useMathGame, ...)  │
├─────────────────────────────────────────┤
│         Components Layer                │
│ (ChallengeZone, CharacterSelect, etc.)  │
└─────────────────────────────────────────┘
```

## Components

### App.vue
- **Purpose:** Main entry point and root layout container.
- **Location:** `src/App.vue`

### ChallengeZone.vue
- **Purpose:** Displays the current math challenge.
- **Location:** `src/components/ChallengeZone.vue`

### CharacterSelect.vue
- **Purpose:** Allows the user to select their character/mascot.
- **Location:** `src/components/CharacterSelect.vue`

### ConfettiBurst.vue
- **Purpose:** Visual celebration effect via canvas-confetti.
- **Location:** `src/components/ConfettiBurst.vue`

### LevelUpModal.vue
- **Purpose:** Modal to display when the user levels up.
- **Location:** `src/components/LevelUpModal.vue`

### MascotPanel.vue
- **Purpose:** Panel to display the currently selected mascot.
- **Location:** `src/components/MascotPanel.vue`

### NumberPad.vue
- **Purpose:** Input mechanism for answers.
- **Location:** `src/components/NumberPad.vue`

### ScoreHeader.vue
- **Purpose:** Displays current score and related stats.
- **Location:** `src/components/ScoreHeader.vue`

### useMathGame.js (Composable)
- **Purpose:** Core game logic, scoring, and level management.
- **Location:** `src/composables/useMathGame.js`

### useSound.js (Composable)
- **Purpose:** Handles game sound effects.
- **Location:** `src/composables/useSound.js`

## Data Flow

1. User interacts with UI components (e.g., NumberPad, CharacterSelect).
2. Input events are routed to the `useMathGame` composable.
3. State changes in the composable trigger Vue reactivity, automatically updating the visible components.
4. Additional effects like sounds (`useSound`) or UI celebrations (`ConfettiBurst`) flow from the changed state or direct function calls.

## Integration Points

No external API or database integrations detected. Purely client-side data.

## Technical Debt

No specific technical debt item markers (TODO, FIXME, etc.) found in the standard source files.

## Conventions

**Naming:** PascalCase for UI components (`ChallengeZone.vue`), camelCase for logic functions/composables (`useMathGame.js`).
**Structure:** Feature-based functional separation (`components/` for UI blocks, `composables/` for reusable logic).
**Testing:** No test directories or test patterns were observed (e.g., missing tests/, specs/).
