---
phase: 2
plan: 2.1
wave: 1
---

# Plan 2.1: Implement Routing & Level Logic

## Objective
Update `useMathGame.js` composable to introduce an 8-level state tracked in `localStorage`, and create the proxy routing logic `getCutsceneVideoPath(characterId, currentLevel)`.

## Context
- src/composables/useMathGame.js
- .gsd/ROADMAP.md

## Tasks

<task type="auto">
  <name>Update Composable Level tracking</name>
  <files>src/composables/useMathGame.js</files>
  <action>
    - Ensure a new reactive `level` ref is tracked up to `8` using `localStorage` persistence.
    - Export `level` from the composable.
    - Write a `getCutsceneVideoPath(characterId, currentLevel)` pure function to return our video path layout, falling back to `/videos/hero.mp4` for now.
  </action>
  <verify>npm run build</verify>
  <done>Exported functions exist and are properly linked to `App.vue` if required.</done>
</task>
